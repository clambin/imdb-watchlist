name: staged build

on:
  push:
    branches:
      - docker

jobs:
  build:
    uses: clambin/workflows/.github/workflows/docker-build.yaml@docker
    with:
      image: ghcr.io/clambin/imdb-watchlist
      dockerfile: build/Dockerfile-staged
