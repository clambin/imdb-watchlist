name: Test

on:
  push:
    branches-ignore:
      - master
      - workflows
  pull_request_target:

jobs:
  analyse:
    uses: clambin/workflows/.github/workflows/analysis.yaml@main
    with:
      go-version: 1.17
  test:
    uses: clambin/workflows/.github/workflows/test.yaml@main
    with:
      go-version: 1.17
